% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamma_functions.R
\name{gamma_functions}
\alias{gamma_functions}
\alias{tgamma}
\alias{tgamma1pm1}
\alias{lgamma_boost}
\alias{digamma_boost}
\alias{trigamma_boost}
\alias{polygamma}
\alias{tgamma_ratio}
\alias{tgamma_delta_ratio}
\alias{gamma_p}
\alias{gamma_q}
\alias{tgamma_lower}
\alias{tgamma_upper}
\alias{gamma_q_inv}
\alias{gamma_p_inv}
\alias{gamma_q_inva}
\alias{gamma_p_inva}
\alias{gamma_p_derivative}
\title{Gamma Functions}
\usage{
tgamma(z)

tgamma1pm1(z)

lgamma_boost(z)

digamma_boost(z)

trigamma_boost(z)

polygamma(n, z)

tgamma_ratio(a, b)

tgamma_delta_ratio(a, delta)

gamma_p(a, z)

gamma_q(a, z)

tgamma_lower(a, z)

tgamma_upper(a, z)

gamma_q_inv(a, q)

gamma_p_inv(a, p)

gamma_q_inva(z, q)

gamma_p_inva(z, p)

gamma_p_derivative(a, z)
}
\arguments{
\item{z}{Input numeric value for the gamma function}

\item{n}{Order of the polygamma function (non-negative integer)}

\item{a}{Argument for the incomplete gamma functions (must be positive)}

\item{b}{Denominator argument for the ratio of gamma functions}

\item{delta}{Increment for the ratio of gamma functions}

\item{q}{Probability value for the incomplete gamma functions (0 ≤ q ≤ 1)}

\item{p}{Probability value for the incomplete gamma functions (0 ≤ p ≤ 1)}
}
\value{
A single numeric value with the computed gamma function, logarithm, digamma, trigamma, polygamma, or incomplete gamma functions.
}
\description{
Functions to compute the gamma function, its logarithm, digamma, trigamma, polygamma,
and various incomplete gamma functions.

The gamma function is a fundamental special function that extends the factorial
function to real and complex numbers.

\strong{Gamma Function Γ(z):}
\itemize{
\item \code{tgamma(z)}: Computes Γ(z), the true gamma function
\item Key mathematical relationships:
\itemize{
\item Recursion formula: Γ(z) = Γ(z+1)/z
\item Reflection formula: Γ(z)·Γ(1-z) = π/sin(πz)
\item Identity: Γ(z+1) = z·Γ(z)
}
\item Implementation uses Lanczos approximation for standard precision and
Stirling's approximation for large z
\item \code{tgamma1pm1(z)}: Computes Γ(1+z) - 1 with enhanced numerical stability
for very small z values, avoiding precision loss
\item \code{lgamma_boost(z)}: Returns log(Γ(z)), the logarithm of the gamma function
}

\strong{Derivative Functions:}
\itemize{
\item \code{digamma_boost(z)}: The digamma (psi) function ψ(z) = d/dz\[ln(Γ(z))\],
the logarithmic derivative of the gamma function
\itemize{
\item Reflection formula: ψ(1-z) = ψ(z) + π/tan(πz)
\item Recurrence relation: ψ(z+1) = ψ(z) + 1/z
}
\item \code{trigamma_boost(z)}: The trigamma function, the second derivative of ln(Γ(z))
\item \code{polygamma(n, z)}: The nth derivative of the digamma function (n-th order polygamma)
}

\strong{Ratios:}
\itemize{
\item \code{tgamma_ratio(a, b)}: Computes Γ(a)/Γ(b)
\item \code{tgamma_delta_ratio(a, delta)}: Computes Γ(a)/Γ(a + delta)
}

\strong{Incomplete Gamma Functions:}

These functions require a > 0 and z ≥ 0.
\itemize{
\item \strong{Normalized (Regularized) Functions} (return values in \eqn{[0, 1]}):
\itemize{
\item \code{gamma_p(a, z)}: Normalized lower incomplete gamma P(a, z).
Changes rapidly from 0 to 1 around z = a
\item \code{gamma_q(a, z)}: Normalized upper incomplete gamma Q(a, z).
Changes rapidly from 1 to 0 around z = a
\item Note: P(a, z) + Q(a, z) = 1
}
\item \strong{Non-normalized Functions} (return values in \eqn{[0, Γ(a)]}):
\itemize{
\item \code{tgamma_lower(a, z)}: Full lower incomplete gamma function
\item \code{tgamma_upper(a, z)}: Full upper incomplete gamma function
}
}

\strong{Inverse Functions:}
\itemize{
\item \code{gamma_p_inv(a, p)}: Returns z such that p = P(a, z)
\item \code{gamma_q_inv(a, q)}: Returns z such that q = Q(a, z)
\item \code{gamma_p_inva(z, p)}: Returns a such that p = P(a, z)
\item \code{gamma_q_inva(z, q)}: Returns a such that q = Q(a, z)
}

\strong{Derivative:}
\itemize{
\item \code{gamma_p_derivative(a, z)}: Computes the derivative of the normalized
lower incomplete gamma function with respect to z
}
}
\examples{
\dontrun{
# Gamma function for z = 5
tgamma(5)
# Gamma function for (1 + z) - 1, where z = 5
tgamma1pm1(5)
# Logarithm of the gamma function for z = 5
lgamma_boost(5)
# Digamma function for z = 5
digamma_boost(5)
# Trigamma function for z = 5
trigamma_boost(5)
# Polygamma function of order 1 for z = 5
polygamma(1, 5)
# Ratio of gamma functions for a = 5, b = 3
tgamma_ratio(5, 3)
# Ratio of gamma functions with delta for a = 5, delta = 2
tgamma_delta_ratio(5, 2)
# Normalized lower incomplete gamma function P(a, z) for a = 5, z = 2
gamma_p(5, 2)
# Normalized upper incomplete gamma function Q(a, z) for a = 5, z = 2
gamma_q(5, 2)
# Full lower incomplete gamma function for a = 5, z = 2
tgamma_lower(5, 2)
# Full upper incomplete gamma function for a = 5, z = 2
tgamma_upper(5, 2)
# Inverse of the normalized upper incomplete gamma function for a = 5, q = 0.5
gamma_q_inv(5, 0.5)
# Inverse of the normalized lower incomplete gamma function for a = 5, p = 0.5
gamma_p_inv(5, 0.5)
# Inverse of the normalized upper incomplete gamma function with respect to a for z = 2, q = 0.5
gamma_q_inva(2, 0.5)
# Inverse of the normalized lower incomplete gamma function with respect to a for z = 2, p = 0.5
gamma_p_inva(2, 0.5)
# Derivative of the normalized lower incomplete gamma function for a = 5, z = 2
gamma_p_derivative(5, 2)
}
}
\seealso{
\href{https://www.boost.org/doc/libs/latest/libs/math/doc/html/math_toolkit/sf_gamma.html}{Boost Documentation} for more details on the mathematical background.
}
