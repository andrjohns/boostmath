% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamma_functions.R
\name{gamma_functions}
\alias{gamma_functions}
\alias{tgamma}
\alias{tgamma1pm1}
\alias{lgamma_boost}
\alias{digamma_boost}
\alias{trigamma_boost}
\alias{polygamma}
\alias{tgamma_ratio}
\alias{tgamma_delta_ratio}
\alias{gamma_p}
\alias{gamma_q}
\alias{tgamma_lower}
\alias{tgamma_upper}
\alias{gamma_q_inv}
\alias{gamma_p_inv}
\alias{gamma_q_inva}
\alias{gamma_p_inva}
\alias{gamma_p_derivative}
\title{Gamma Functions}
\usage{
tgamma(z)

tgamma1pm1(z)

lgamma_boost(z)

digamma_boost(z)

trigamma_boost(z)

polygamma(n, z)

tgamma_ratio(a, b)

tgamma_delta_ratio(a, delta)

gamma_p(a, z)

gamma_q(a, z)

tgamma_lower(a, z)

tgamma_upper(a, z)

gamma_q_inv(a, q)

gamma_p_inv(a, p)

gamma_q_inva(z, q)

gamma_p_inva(z, p)

gamma_p_derivative(a, z)
}
\arguments{
\item{z}{Input numeric value for the gamma function}

\item{n}{Order of the polygamma function (non-negative integer)}

\item{a}{Argument for the incomplete gamma functions (must be positive)}

\item{b}{Denominator argument for the ratio of gamma functions}

\item{delta}{Increment for the ratio of gamma functions}

\item{q}{Probability value for the incomplete gamma functions (0 <= q <= 1)}

\item{p}{Probability value for the incomplete gamma functions (0 <= p <= 1)}
}
\value{
A single numeric value with the computed gamma function, logarithm, digamma, trigamma, polygamma, or incomplete gamma functions.
}
\description{
Functions to compute the gamma function, its logarithm, digamma, trigamma, polygamma,
and various incomplete gamma functions.

\strong{Gamma Function Gamma(z):}
\itemize{
\item \code{tgamma(z)}: Computes \eqn{\Gamma(z)}, the true gamma function
\item \code{tgamma1pm1(z)}: Computes \eqn{\Gamma(1+z) - 1} with enhanced numerical stability
for very small z values, avoiding precision loss
\item \code{lgamma_boost(z)}: Returns \eqn{\log(\Gamma(z))}, the logarithm of the gamma function
}

\strong{Derivative Functions:}
\itemize{
\item \code{digamma_boost(z)}: The digamma function, the first derivative of the logarithm of the Gamma function
\eqn{\psi(z) = \frac{d}{dz}\log\Gamma(z) = \frac{\Gamma'(z)}{\Gamma(z)}},
\item \code{trigamma_boost(z)}: The trigamma function, the second derivative of the logarithm of the Gamma function
\eqn{\psi_1(z) = \frac{d^2}{dz^2}\log\Gamma(z)}
\item \code{polygamma(n, z)}: The nth derivative of the digamma function (n-th order polygamma) \eqn{\psi^{(m)}(z) = \frac{d^m}{dz^m}\psi(z)}
}

\strong{Ratios:}
\itemize{
\item \code{tgamma_ratio(a, b)}: Computes \eqn{\Gamma(a)/\Gamma(b)}
\item \code{tgamma_delta_ratio(a, delta)}: Computes \eqn{\Gamma(a)/\Gamma(a + \delta)}
}

\strong{Incomplete Gamma Functions:}

These functions require a > 0 and z >= 0.
\itemize{
\item \strong{Normalised (Regularised) Functions} (return values in \eqn{[0, 1]}):
\itemize{
\item \code{gamma_p(a, z)}: Normalised lower incomplete gamma \eqn{P(a, z) = \frac{\gamma(a, z)}{\Gamma(z)}}
\item \code{gamma_q(a, z)}: Normalised upper incomplete gamma \eqn{Q(a, z) = \frac{\Gamma(a, z)}{\Gamma(a)}}
}
\item \strong{Non-normalised Functions} (return values in \eqn{[0, Gamma(a)]}):
\itemize{
\item \code{tgamma_lower(a, z)}: Full lower incomplete gamma function \eqn{\gamma(a, z) = \int_{0}^z t^{a-1}e^{-t}dt}
\item \code{tgamma_upper(a, z)}: Full upper incomplete gamma function \eqn{\Gamma(a, z) = \int_{z}^\infty t^{a-1}e^{-t}dt}
}
}

\strong{Inverse Functions:}
\itemize{
\item \code{gamma_p_inv(a, p)}: Returns \eqn{z} such that \eqn{p = P(a, z)}
\item \code{gamma_q_inv(a, q)}: Returns \eqn{z} such that \eqn{q = Q(a, z)}
\item \code{gamma_p_inva(z, p)}: Returns \eqn{a} such that \eqn{p = P(a, z)}
\item \code{gamma_q_inva(z, q)}: Returns \eqn{a} such that \eqn{q = Q(a, z)}
}

\strong{Derivative:}
\itemize{
\item \code{gamma_p_derivative(a, z)}: Computes the derivative of the normalised
lower incomplete gamma function with respect to z: \eqn{\frac{\partial}{\partial z}P(a, z) = \frac{e^{-z}z^{a-1}}{\Gamma(a)}}
}
}
\examples{
\dontrun{
# Gamma function for z = 5
tgamma(5)
# Gamma function for (1 + z) - 1, where z = 5
tgamma1pm1(5)
# Logarithm of the gamma function for z = 5
lgamma_boost(5)
# Digamma function for z = 5
digamma_boost(5)
# Trigamma function for z = 5
trigamma_boost(5)
# Polygamma function of order 1 for z = 5
polygamma(1, 5)
# Ratio of gamma functions for a = 5, b = 3
tgamma_ratio(5, 3)
# Ratio of gamma functions with delta for a = 5, delta = 2
tgamma_delta_ratio(5, 2)
# Normalised lower incomplete gamma function P(a, z) for a = 5, z = 2
gamma_p(5, 2)
# Normalised upper incomplete gamma function Q(a, z) for a = 5, z = 2
gamma_q(5, 2)
# Full lower incomplete gamma function for a = 5, z = 2
tgamma_lower(5, 2)
# Full upper incomplete gamma function for a = 5, z = 2
tgamma_upper(5, 2)
# Inverse of the normalised upper incomplete gamma function for a = 5, q = 0.5
gamma_q_inv(5, 0.5)
# Inverse of the normalised lower incomplete gamma function for a = 5, p = 0.5
gamma_p_inv(5, 0.5)
# Inverse of the normalised upper incomplete gamma function with respect to a for z = 2, q = 0.5
gamma_q_inva(2, 0.5)
# Inverse of the normalised lower incomplete gamma function with respect to a for z = 2, p = 0.5
gamma_p_inva(2, 0.5)
# Derivative of the normalised lower incomplete gamma function for a = 5, z = 2
gamma_p_derivative(5, 2)
}
}
\seealso{
\href{https://www.boost.org/doc/libs/latest/libs/math/doc/html/math_toolkit/sf_gamma.html}{Boost Documentation} for more details on the mathematical background.
}
