% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logistic_functions.R
\name{logistic_functions}
\alias{logistic_functions}
\alias{logistic_sigmoid}
\alias{logit}
\title{Logistic Functions}
\usage{
logistic_sigmoid(x)

logit(x)
}
\arguments{
\item{x}{Numeric value for which to compute the functions}
}
\value{
A single numeric value with the computed logit or logistic sigmoid function.
}
\description{
Functions to compute the logistic sigmoid function and its inverse, the logit function.

These functions are fundamental in statistics, machine learning, and probability theory,
particularly in logistic regression and neural networks.

\strong{Logistic Sigmoid Function:}

The logistic sigmoid (also called the logistic function or expit) is defined as:
\deqn{\sigma(x) = \frac{1}{1 + e^{-x}} = \frac{e^x}{1 + e^x}}
\itemize{
\item \code{logistic_sigmoid(x)}: Computes σ(x)
\item Domain: (-∞, ∞)
\item Range: (0, 1)
\item S-shaped (sigmoid) curve
\item Symmetric around x = 0: σ(-x) = 1 - σ(x)
\item Special values:
\itemize{
\item σ(0) = 0.5
\item σ(∞) = 1
\item σ(-∞) = 0
}
\item Derivative: σ'(x) = σ(x)(1 - σ(x))
}

\strong{Applications:}
\itemize{
\item Activation function in neural networks
\item Models probability in logistic regression
\item Converts log-odds to probabilities
\item Smooth approximation to step functions
}

\strong{Logit Function:}

The logit function (log-odds or inverse sigmoid) is defined as:
\deqn{logit(p) = \log\left(\frac{p}{1 - p}\right)}
\itemize{
\item \code{logit(x)}: Computes logit(x) = log(x/(1-x))
\item Domain: (0, 1)
\item Range: (-∞, ∞)
\item Inverse of the logistic sigmoid: logit(σ(x)) = x
\item Odd function around p = 0.5: logit(1 - p) = -logit(p)
\item Special values:
\itemize{
\item logit(0.5) = 0
\item logit(0) = -∞
\item logit(1) = ∞
}
\item Derivative: d/dp logit(p) = 1/(p(1-p))
}

\strong{Applications:}
\itemize{
\item Converts probabilities to log-odds scale
\item Link function in logistic regression
\item Transforms bounded quantities to unbounded scale
\item Used in odds ratio calculations
}

\strong{Numerical Stability:}

Both functions use numerically stable implementations that avoid overflow/underflow
for extreme values of their arguments.
}
\examples{
# Logistic Sigmoid Function
logistic_sigmoid(0)     # Returns 0.5
logistic_sigmoid(2)     # Returns ~0.881
logistic_sigmoid(-2)    # Returns ~0.119

# Logit Function (inverse of sigmoid)
logit(0.5)              # Returns 0
logit(0.7)              # Returns ~0.847
logit(0.881)            # Returns ~2 (inverse of sigmoid(2))
}
\seealso{
\href{https://www.boost.org/doc/libs/latest/libs/math/doc/html/math_toolkit/logistic.html}{Boost Documentation}
for more details on the mathematical background.
}
